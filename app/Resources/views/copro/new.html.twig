{% extends "admin/base_admin.html.twig"%}
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block body %}
    <h1>Copro creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('copro_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
