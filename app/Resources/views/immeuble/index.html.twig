{% extends "admin/base_admin.html.twig"%}
{% block body %}
    <h1>Immeubles list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Adresse</th>
                <th>Codepostal</th>
                <th>Ville</th>
                <th>Plan</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for immeuble in immeubles.appartements %}
            <tr>
                <td><a href="{{ path('immeuble_show', { 'id': immeuble.id }) }}">{{ immeuble.id }}</a></td>
                <td>{{ immeuble.adresse }}</td>
                <td>{{ immeuble.codePostal }}</td>
                <td>{{ immeuble.ville }}</td>
                <td>{{ immeuble.plan }}</td>
                <td>
                    <ul>
                       t
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('immeuble_new') }}">Create a new immeuble</a>
        </li>
    </ul>
{% endblock %}
